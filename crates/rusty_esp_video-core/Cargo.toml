[package]
name = "rusty_esp_video-core"
description = "The pure no_std + alloc core of rusty_esp_video: types, traits and algorithms with no drivers, no allocator and no product types. forbid(unsafe)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
rust-version.workspace = true
readme = "README.md"
documentation = "https://docs.rs/rusty_esp_video-core"
keywords = ["esp32", "embedded", "no-std", "mata", "remade-with-rust"]
categories = ["embedded", "no-std", "hardware-support"]

[features]
default = ["std"]
# The feature ladder is the rusty_zstd one: `std` implies `alloc`; with neither,
# the crate is pure `core` and still builds on riscv32 bare metal.
std = ["alloc", "rusty_esp_core/std"]
alloc = ["rusty_esp_core/alloc"]

[dependencies]
rusty_esp_core = { workspace = true }

[lints]
workspace = true
