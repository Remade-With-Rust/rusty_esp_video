// Extra ESP-IDF component headers to bind (see Cargo.toml
// [[package.metadata.esp-idf-sys.extra_components]]). Generated into
// `esp_idf_sys::camera`, which rusty_esp_image-esp reads.
#include "esp_camera.h"
